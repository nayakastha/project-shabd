name: shabd
description: INDyPress
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: 
    sdk: flutter
  intl: ^0.17.0    
  lottie: ^1.1.0
  get: ^4.1.4
  get_storage: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/animations/
    - lib/services/i18n/lang
